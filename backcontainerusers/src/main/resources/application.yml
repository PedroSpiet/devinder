spring:
  jpa:
    database: mysql
    show-sql: true
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/devinder?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=false&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: docker

  application:
    name: backcontainerusers